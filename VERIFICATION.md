# 验证状态与历史记录

## 当前状态（2026-09-11，局内成长与最终 Boss）

协议 v17、zombie 插件 1.2.0。全量 `node --test tests/*.test.cjs` **178/178 通过**（约 12 秒）；随后补充延后升级/预警圈复用测试、调整燃烧区判定为与地面圆圈一致，`node --test tests/roguelike.test.cjs tests/pve.test.cjs` **22/22 通过**。当前目录共 180 项测试，未再次重复全量。新增模块语法、数值文档与 `git diff --check` 通过。

覆盖共享经验只计一次、个人独立多次升级、前置技能过滤、旧 offerId 重放拒绝、随机选项检查点恢复、穿透/弹射/闪电与遮挡、扩大爆炸/持续燃烧/有界次爆、脉冲减速与碎冰增伤、Boss 7 分钟登场/队友复活/1.5 秒预警/重叠圈仅命中一次/超时失败，以及状态包与快照边界。页面事件测试验证经验显示、U 暂存、选择换装、Boss 登场与撤离结算；显示对象测试验证预警圈与火区复用及清理。

本轮未运行构建、开发服务、监听、Docker 或真实 Redis。没有安装依赖或下载素材。验证使用真实核心和 Three.js 几何、模拟传输/DOM/渲染器；实际 WebGL 画面、声音和公网多人平衡仍需部署试玩。玩法见 ROGUELIKE.md。

## 历史：2026-09-11，五种僵尸与人数缩放

协议 v16，zombie 插件 1.1.0。`node --test tests/*.test.cjs` **170/170 通过**，约 11.7 秒；数值文档与 `git diff --check` 通过。

新增覆盖 1–8 人血量缩放、阵亡不降难度、正式离场保持血量比例、死亡敌人不复活、五种类型按波次解锁、各类型实际移动速度与近战伤害/间隔、8 人混合尸群的副本及检查点恢复、非法类型/人数/生命拒绝，以及五种原创模型的几何预算/差异/资源释放。原 PvP、PvE 页面、寻路和网络回归仍通过。

本轮未打包、未启动服务、未操作 Docker，未做真实浏览器画面或公网多人验收。部署试玩时请检查不同类型的识别度、8 人集火时的击杀节奏、队友阵亡与离场后的血条变化。

## 历史：2026-09-11，PvE 首版

协议 v15。全量 `node --test tests/*.test.cjs` 为 **165/165 通过**（约 11.5 秒）；随后补充离场奖励清理与内部僵尸 ID 保留检查，`node --test tests/pve.test.cjs tests/ui.test.cjs` **10/10 通过**。当前测试目录共 166 项；最后一次变更后没有再重复执行全量。数值文档检查、新增模块语法检查与 `git diff --check` 通过。

新增覆盖：8 分钟结算、单人 PvE 与 8 人容量、人类＋小手枪开局、直击/爆炸/自伤豁免、僵尸近战和跨层爬坡、有限敌人池、清波复活、全灭失败、三选一领取与重复/旧 epoch 拒绝、装填/自愈/脉冲效果、检查点确定性、重连和广播包 64 KiB 上限。使用真实页面脚本、Three.js 对象和 DOM/渲染器/传输替身，走通合作房间创建、出怪、升级换装、HUD 和胜利结算。原 FFA 900 tick 行为基线仍通过。

本轮没有执行打包、构建、服务监听、Docker 操作或真实 Redis 连接。没有新增依赖或下载模型素材。真实浏览器 WebGL 视觉、多人公网手感、10 Hz PvE 插值及目标服务器容量仍需部署试玩；替身测试不代表真实网络或画面已经验收。

验收入口：房间创建模式选「僵尸合作生存」→ 创建 → 准备 → 房主开局（允许单人）。确认小手枪、第一波感染者、近战受击、清波三选一（1/2/3）、队友清波复活与 8 分钟胜利。联机至少再用两台设备检查无友伤、掉线恢复和同步。

以下为开发历史记录，旧数值/协议/测试数量不代表当前版本。

## 历史：直坡驾驶与鼠标镜头阶段

直坡驾驶改造后：62 项自动测试通过，0 项失败。

执行过的轻量检查：

- node --test --test-reporter=spec tests/*.test.cjs
- 全部 23 个项目 JS/CJS 文件的 node --check
- HTML 本地资源、重复 ID、画面代码引用的元素检查
- package.json 与 package-lock.json 依赖一致性、Docker COPY 来源文件检查
- docker compose --env-file .env.example config --quiet（通过，只解析配置）

覆盖范围：

- 原有 9 种车体/武器组合、驾驶瞄准、弹丸/激光、跨层碰撞、四条可驾驶直坡、坡上停车/倒车/交战、楼板开口与坡面挡弹、AI、确定性快照。
- 8 人房间、准备/房主权限、容量、插件核对、重连凭证、输入短按边沿、离开淘汰、房间隔离、重开 epoch。
- 浏览器 NetworkSession 与 RoomServer 通过内存传输替身连接，验证驾驶和仅暂停自身操作。
- 使用真实 Three.js 场景对象、DOM 与渲染器替身执行页面逻辑，验证创建房间、开局、快照渲染调用、直接驾驶上下坡、键盘不转炮塔、离开回本地。
- HTTP 请求处理器通过流替身验证静态资源与健康检查；WebSocket 事件适配层通过事件替身验证收发和断开。
- Redis 客户端替身验证独占租约、保存、失去租约拒写、释放和超时；真实战斗检查点的恢复与清空操作。

没有执行：真实端口监听、Redis 网络连接、Docker build / compose up、实际浏览器 WebGL 视觉验收、两台机器或 8 人公网测试。浏览器此前的本地文件策略限制未被绕过。替身测试不证明真实网络握手、Redis Lua 执行或 Docker 运行已通过。

部署后待验收：通过服务器网址两人加入，移动/开火/连续斜坡驾驶，暂停仍可受击，断线 30 秒内恢复、超时淘汰，Redis 启用后的容器重启恢复，以及弱网操控延迟。步骤见 DEPLOY.md。

直坡专项覆盖：所有车体通过全部四条坡道并倒回；禁止侧面驶入/驶出；坡上坦克仍可被命中；坡中快照恢复保持确定性；伪造坡上高度被拒绝；AI 实际爬坡到达上一层。旧自动楼梯测试已由这些用例替换，因此测试数与上一版不同。协议升级至 v4，Redis 默认前缀为 tiger:rooms:v4。

鼠标镜头修复：UI 替身测试验证固定 clientX/clientY 下使用 movementX 连续旋转超过 360°、俯仰保持固定枢轴和半径、释放指针锁定后暂停操作。真实浏览器 Pointer Lock 授权与手感仍需用户刷新后验收。

## 2026-09-09：计分混战、补给与反馈

- `node --test tests/*.test.cjs`：68/68 通过，约 10 秒。随后增加 AudioContext mock 检查并再次通过 UI 专项；`node --check battle.js`、`node --check battle-core.js` 和 `git diff --check` 通过。
- 新增用例覆盖四秒复活、空闲出生点等待、两秒保护与开炮解除、离场不复活、15 次击毁结束、时间到平局与死亡数破同分、跨楼层补给判定、加速实际速度、20 秒补给刷新、补给/复活快照恢复与非法数据拒绝。
- 原完整比赛测试已按五分钟计分制更新；保留坡道驾驶、AI 跨楼层寻路、相机 360 度和固定支点俯仰的回归测试。
- UI 测试使用真实 Three.js 数学和几何、模拟 DOM/WebGLRenderer/WebSocket/AudioContext，检查死亡不弹出阻塞菜单、自动复活、计分板、结算与音频参数。它不替代真实浏览器渲染或试听。
- 本轮实际浏览器检查被环境阻挡：Mac 锁屏，浏览器连接报告 unsupported Codex auth method: apikey。没有启动服务、构建镜像或进行真实 Redis/异机八人测试。
- 协议 v5，地图 summer-crossfire-v3；Redis 默认前缀 tiger:rooms:v5。重启 Node 并刷新全部客户端后生效，旧协议对局不能恢复为本版。

## 2026-09-10：护盾与三种机甲技能

`node --test tests/*.test.cjs`：74/74 通过，约 14 秒。随后补充 Shift 经页面事件进入联机权威核心的检查，UI 专项再次通过。`node --check battle.js`、`git diff --check` 通过。

新增测试覆盖护盾优先吸收及伤害溢出、五秒延迟恢复与暂停、冲刺距离/同时射击/墙和坡道侧面碰撞、技能按键边沿、临时屏障消耗和到期、正面 120° 减伤与炮塔朝向、部署减速、技能快照恢复、非法护盾/输入拒绝，以及三种武器对中型机甲的持续命中击毁时间。旧数值测试已按新版装甲、护盾及武器伤害更新。

本轮没有启动或重启服务、构建 Docker 镜像或进行实际浏览器试玩与试听。UI 验证使用模拟渲染器和 AudioContext，不代表实机手感验证。协议升级 v6，六个内容插件为 2.0.0；需要重启服务器并刷新客户端，显式 Redis 前缀应改为 tiger:rooms:v6。

## 2026-09-10：按激光 2／3／4 发重新平衡

74/74 测试通过，约 14 秒。新的武器平衡回归用例实际运行蓄力、装填、射线/弹丸碰撞和护盾吸收，检查全部九种武器与目标组合：满蓄力激光击毁轻/中/重型需要 2/3/4 次命中，标准炮 6/9/12 次，快速炮 23/34/45 次。条件是满装甲满基础护盾、连续命中、不使用技能/补给。技能或脱战护盾恢复可能改变枪数。

装甲/护盾分别为 140/60、210/90、280/120；伤害为激光 100、标准 35、快速 9。先锋临时护盾调整为 60，维修包 40，装填和技能时间不变。插件版本 2.1.0、协议 v7，Redis 默认前缀 tiger:rooms:v7，防止旧平衡版本检查点混用。JavaScript 语法和 git diff 空白检查通过；未运行服务、构建命令或实际浏览器试玩。

## 2026-09-10：联机输入边沿与状态停更

78/78 测试通过，约 14 秒；network-session.js、room-server.js 语法检查及 git diff --check 通过。
新增回归检查：同一个模拟 tick 前到达的技能按下/松开及同时开火、瞄准合并、取消输入清队列；1.5 秒停更只提示一次并停止输入、相同 tick 不能掩盖停更、恢复后需手动继续、5 秒关闭旧连接并携带凭证重连、普通暂停/大厅/结算不误触发。页面事件测试同时验证超时弹出暂停界面、过期状态不能继续、恢复提示与继续按钮。
本轮没有启动监听服务、构建 Docker 或执行真实弱网/异机联机测试。模拟测试不能替代部署实测。重启现有 Node 服务并刷新客户端即可加载修复，协议和 Redis 前缀保持 v7。

## 2026-09-10：文档一致性整理

合并 README 重复属性表，更新控制说明、v7 协议示例、插件技能字段、正常五分钟赛制与十五分钟异常兜底的区别，并补全联机超时、部署验收和已知边界。此轮仅修改文档，未重新运行游戏测试或启动服务；78 项为最近一次代码验证结果。

## 2026-09-10：人类与火箭筒

85/85 测试通过，约 13 秒。随后扩展页面用例，验证车库选择人类 + 火箭筒、80 血 HUD、侧移与暂停；UI、插件、步兵专项共 16 项再次通过。JavaScript 语法及 diff 空白检查通过。

新增覆盖：人类小碰撞体及激光一击击毁、相对镜头侧移与斜向限速、闪避撞墙且无无敌、火箭直接命中的 20 + 80 分段伤害、爆炸衰减/范围/墙和楼板遮挡、自伤与无自杀奖励、爆炸事件复制及检查点恢复。全组合测试循环现已覆盖 4 × 4 配置，所有单位通过四条坡道及返回路径。

此轮未启动服务或构建 Docker，未进行实际浏览器视觉、动画、音效或真实联机验收。更新后需要重启 Node 并刷新客户端；Docker 部署需要更新镜像。显式 Redis 前缀改为 tiger:rooms:v8，旧检查点不兼容。README、多人协议、部署和插件契约同步更新。

## 2026-09-10：架构拆分与配置统一

`node --test tests/*.test.cjs`：90/90 通过。新增回归将拆分前保存的 900 tick 完整模拟结果作为基准，只排除版本号和清单格式，逐字段验证玩法未改变。其余新增测试验证统一资源清单、顺序加载与失败提示、网络状态不携带 AI 路径/配置清单、广播状态不能恢复权威对局、技能参数与 README 自动数值表一致。

原有 85 项保留，覆盖全部 16 种搭配、坡道、鼠标镜头、火箭爆炸、房间及输入校验。此次只做轻量测试、语法与空白检查、文档生成和代码格式化；没有启动监听、打包或构建，也没有新增真实浏览器、Redis 或 Docker 实测。

协议升级至 v9，Redis 默认前缀同步更新；旧检查点不可直接恢复。架构入口见 ARCHITECTURE.md。历史阶段的数量、数值和协议保留作为记录。

## 2026-09-10：修复开火后渲染卡顿

架构拆分时 createUnits 遗漏 floorGroups 依赖，激光预警线进入遮挡检测后抛出 ReferenceError，使当帧后续渲染与特效清理被跳过。本地 AI 也会触发，因此玩家使用火箭筒时同样可能遇到。已在页面初始化处显式传入楼层组。

扩展原 UI 用例：在本地 AI 对战中分别使用火箭筒和激光，持续开火 360 帧并推进松开后的 90 帧。修改前稳定复现 floorGroups is not defined，修复后 UI 专项通过；两个修改的运行文件语法及 diff 空白检查通过。此次未重跑完整 90 项，未启动服务、打包或进行真实 GPU 帧率测量。协议和数值不变。

## 2026-09-10：公开房间列表 UI

92/92 自动测试通过。新增房间目录和浏览器列表专项，验证人数/阶段/可加入状态、不公开重连 token、空列表、文本安全渲染、加入回调、连接期间禁用、仅可见时定时刷新、失败清空旧列表与重试恢复。HTTP 替身验证 /api/rooms 的 JSON 响应与 no-store；原 UI 回归继续验证创建房间、准备开局及离开流程。

增加黑色半透明面板、可滚动房间卡片、状态与人数、新建房间和刷新入口。没有启动服务或运行构建。尝试连接现有 localhost:8080 进行视觉检查时连接被拒绝，因此本次未完成真实浏览器及真实 HTTP 联调。新增 HTTP 接口需重启服务；协议与 Redis 前缀不变。

## 2026-09-10：前端混淆发布流程

95/95 自动测试通过。新增发布清单、私有源码与 source map 隔离、发布路径不回退以及小型内存混淆示例测试；示例验证实际 javascript-obfuscator 输出保持跨模块接口、中文值与 JSON 字段，并可在禁止动态字符串代码生成的 VM 中执行。它们不构建整个游戏。

安装构建依赖时禁用了安装脚本；同步 package-lock。运行文件及生成器语法、diff 空白检查通过。未运行 build:client、Docker build、开发服务或监听命令，未生成 public-dist，完整游戏混淆兼容性与帧率仍待发布版本验收。协议与 Redis 前缀保持 v9。

## 2026-09-10：直坡下方通行

100/100 测试通过。新增五项专项，内部循环覆盖四种单位与四条坡道：实际输入驶入/倒车退出、保持平地状态、低净空及完整占地范围阻挡、人类体型差异、坡上坡下同时有车、水平射线通行与向上坡面挡弹、火箭射击、AI 路径、镜头坡底碰撞、网络状态接收、非法净空拒绝和检查点恢复后连续输入一致性。原有爬坡、侧向护栏、900 tick 拆分基准等测试保留且通过。

规则改变，协议升级 v10，默认 Redis 前缀同步更新；旧版检查点不兼容。没有新增跳跃或下落。此次没有启动服务、构建前端或 Docker，未完成真实浏览器视觉/驾驶手感验收。更新部署后需使用新发布文件、重启服务并刷新客户端。

## 2026-09-10：浏览器创建房间 Illegal invocation

NetworkSession 默认直接保存原生 clearTimeout / setTimeout，再通过 this.cancel / this.schedule 调用，导致浏览器原生方法收到会话对象作为接收者，创建连接或重连时可能抛出 Illegal invocation。改为通过 globalThis 调用的包装函数，保留测试注入接口。

新增带浏览器接收者约束的定时器测试：修改前在 connect 的 clearTimeout 处复现相同错误，修复后创建请求、断线重连与关闭均通过。网络及页面专项共 14/14 通过；语法与 diff 空白检查通过。本轮未重跑完整测试、启动服务或构建发布文件。协议保持 v10，源码部署刷新页面；混淆/Docker 部署需要更新发布产物。

## 2026-09-10：空格开火

空格改为开火，与左键和 F 共用输入；取消原空格刹车映射。激光仍按住蓄力、松开释放。页面控制提示和 README 同步更新，UI 回归覆盖空格操作火箭筒/激光以及 F 开火并通过。语法及空白检查通过；未执行构建，协议保持 v10。

## 2026-09-10：指定出口下落

107/107 自动测试通过。新增六项专项，包含四种单位、两层、南北出口的 16 种下落路径，无摔落伤害、封闭边缘阻挡、导航避开出口、空中驾驶输入无效但瞄准开火有效、真实中间楼板承接、落点占用避让、途中死亡与复活、网络字段及非法状态校验、空中检查点恢复后逐 tick 一致性。页面替身回归增加实际联机输入进入下落、HUD 显示及落地继续渲染的检查。

新增状态明确加入广播/恢复格式，协议 v11，旧检查点不兼容。900 tick 原字段基准保留，新增字段在该场景必须为非下落状态。此次未启动服务、运行构建或做真实浏览器视觉/性能验收；发布部署需要更新产物并重启。

## 2026-09-10：红色目标反馈与宽激光

111/111 自动测试通过。新增四项回归：细射线原本打不中的近失目标可被宽激光命中、超出宽度仍不命中、墙和坡面挡光束、真实 Three.js 几何的目标射线识别、红色车体/名称及原色恢复、墙/死亡/隐藏辅助几何排除，以及显示半径匹配权威事件。原有激光 2/3/4 枪数、坡道、下落及联机回归继续通过。

只执行自动测试与语法/空白检查，未构建发布文件或镜像，也未完成真实 WebGL 手感验收。协议升级 v12；发布部署需更新产物、重启并刷新客户端。

## 2026-09-10：静止鼠标辅助锁定

115/115 自动测试通过。新增四项专项验证静止延迟、中心校准、目标跟随、真实鼠标修订立即解除与延迟重新获取、遮挡/死亡/超距/离开视野/暂停解除、直接命中优先与保持目标不乱切换；返回值不含开火命令。原页面事件与几何红色反馈测试继续通过。

辅助范围有限，准星移动但镜头仍由鼠标控制；权威模拟和协议保持 v12。本轮未执行构建、启动服务或完成实机辅助瞄准手感验收；发布版需要更新产物。

## 2026-09-10：卡通机甲与素材武器

- 使用 Kenney Blaster Kit 2.1 的 b/j/r/h 四个 CC0 模型，保留源许可和 ZIP 哈希，校正 -Z → -X 方向与炮口中心；不依赖运行时贴图请求或外部 CDN。
- 新增三项真实 Three.js 几何测试，覆盖全部 16 种搭配、炮口水平距离、旋转父子关系、初始护盾隐藏、有限顶点/法线、索引与颜色、5000 三角形 / 26 可见网格预算、源码/发布资源白名单。原组合测试改用真实几何并验证 dispose，UI 测试继续覆盖开火、激光/火箭爆炸和瞄准标记。
- `node --test tests/*.test.cjs`：118 项；900 tick 权威模拟基线不变。
- `docs/cartoon-lineup.png` 是从真实模型三角形离线投影、深度测试生成的外形预览，不是游戏截图，也不等同于 WebGL 阴影、色调和帧率验收。
- 浏览器工具因 `unsupported Codex auth method: apikey` 无法连接；未启动服务、未打包、未构建 Docker、未完成真实浏览器或异机联机验收。
- 协议与 Redis 前缀仍为 v12，插件补丁版本统一更新；旧插件清单需更新服务器/客户端并新建房间。

## 2026-09-10：实录坦克行驶声

- 行驶声接入 Freesound/qubodup #187676，CC0；使用公开高质量 MP3 预览，处理为单声道 24 kHz、100 ms 首尾交叉渐变的循环，输出约 56 KiB。来源及哈希见 AUDIO.md。未采用中途试写的合成动力声。
- 三项音频单元测试验证加载去重、唯一循环声源、按速度调节、超速/倒车音量上限、暂停/人类/死亡/下落静音、错误重试、暂停后异步加载完成不误播放。音频与 UI 专项使用 AudioContext 替身，不代表听感验收。
- HTTP 流替身验证 MP3 MIME 与原文件字节完全一致；源码/发布路径、Docker 文件清单与资源检查覆盖新增 assets。未执行构建或启动服务。
- 完整测试：121/121 通过。原战斗数值与协议 v12 不变。真实浏览器播放、循环听感和设备音量仍需用户验收。

## 2026-09-10：程序化卡通武器

- 四个武器外观改为 Three.js 圆柱/倒角方块组合，移除 157,694 字节外部几何数据、导入脚本和原许可文件。车体和真实行驶录音保持不变。
- 三项模型测试更新为全部 16 种搭配的水平炮口距离、旋转枢轴、有限几何、四种不同外形尺寸、每款少于 6000 三角形/最多 30 可见网格，以及移除资源在源码/发布模式均不可访问。原资源释放与开火回归继续执行。
- 离线几何预览已更新；没有启动开发服务、构建或进行真实 WebGL 视觉验收。协议仍 v12，四个武器插件补丁版本提升，服务器与客户端需同时更新并新建房间。
- 本轮 `node --test tests/*.test.cjs`：121/121 通过，约 12.5 秒；语法检查和 `git diff --check` 通过。

## 2026-09-10：Blender 维修基地样板

`node --test tests/*.test.cjs`：125/125 通过，约 12 秒。新增模型边界与闭合正面射线检查、几何共享、标线不跨楼板边缘或坡道开口、资源加载失败回退；扩展 HTTP 替身验证 JSON 内容/MIME、发布清单包含模型、Blender 原件不公开。

Blender 5.2.1 LTS 后台以 2 线程生成简单模型，源文件约 179 KiB、网页 JSON 约 573 KiB；已在 Blender 界面打开检查。没有运行图像渲染、项目构建、开发服务或 Docker；实际游戏 WebGL 视觉与帧率仍待验收。

## 2026-09-10：Blender 机甲与武器

`node --test tests/*.test.cjs`：129/129 通过，约 13 秒。新增全部 16 种 Blender 搭配的炮口、关节、轮心、几何与绘制预算检查，缓存单次请求、残骸销毁隔离、未知插件回退、加载期间切换单位；HTTP 替身检查模型 JSON 与发布清单，源 .blend 不公开。

已生成并检查真实几何离线预览 `docs/blender-arsenal.png`，不等同于实际浏览器截图。本轮未运行项目打包、开发服务、Docker 或 Blender 图像渲染；实际 WebGL 画面和帧率待验收。

## 2026-09-10：固定延时激光

激光 trigger 改为 delayed：点击后固定预热 90 tick，power 恒为 1，发射后装填 144 tick。保留其他插件的 charge 模式。协议 v13、激光 2.3.0、Redis 默认前缀 tiger:rooms:v13。

测试覆盖短按与不同松开时刻均在第 90 tick 发射、固定威力、长按不连发、网络短按、预热中检查点恢复、断线与输入超时取消、原 2/3/4 发击毁基准。由于 AI 激光射击时机变化，900 tick 确定性基线已明确更新为 v13；不再称作重构前基线。未启动服务或打包，实际联机手感待验收。

## 2026-09-10：快速炮与激光装填平衡

快速炮伤害 9 → 13，射击间隔仍为 18 tick；激光装填 144 → 126 tick（2.1 秒），预热 90 tick 和固定 100 基础伤害不变。插件 rapid 2.1.3、laser 2.3.1；协议及 Redis 前缀保持 v13，插件清单拒绝旧数值客户端与检查点。

全量检查 130/131 通过，唯一差异为数值变化导致的 900 tick AI 对战快照；核对并更新该基线后，架构专项 5/5 通过，所有 131 项用例已通过验证。快速炮满血满盾击毁轻/中/重型需 16/24/31 发，人类需 7 发。数值文档已生成，未运行构建或启动服务。

## 2026-09-10：标准炮强化弹

全量 136/136 测试通过；随后补充界面进度与炮口光检查，UI 专项再次通过。覆盖 35/35/70 循环、强化打空消耗、墙和保护目标不积攒、护盾命中、死亡清零、前一条命弹丸、检查点与网络投影、非法进度/伤害/强化输入拒绝。数值文档与 900 tick v14 基线已同步；普通弹累计改变实体快照是预期差异。

协议 v14，standard 2.2.0，Redis 前缀 tiger:rooms:v14。实际浏览器光晕与命中反馈、异机联机仍待体验；本次未构建或启动服务。

## 2026-09-10：镜像发布 Compose 配置

主配置改为拉取镜像、restart always、60 秒健康检查；Redis 默认地址 redis://redis:6379/0，修正过期默认前缀为 v14。增加可选构建与外部 Redis 网络叠加文件。使用 docker compose config 解析主配置、构建叠加、网络叠加并检查端口、健康检查、Redis 前缀与混淆开关；未运行构建、推送、拉取镜像或启动容器。镜像仓库、服务器网络/域名与架构需按实际部署设置。

## 2026-09-10：固定前缀恢复与单文件 Compose

新增 Redis 恢复专项：兼容房间保留席位/重连凭证；协议或插件不兼容时备份原始数据并重置空大厅，最近一份备份 TTL 24 小时；空库正常启动；JSON 损坏、非法当前数据、锁冲突、租约丢失、并发修改或 Redis 写入失败不清理原检查点。测试使用 Redis 客户端替身，实际 Lua/Redis 网络执行仍待部署验收。

保留用户的 docker-compose.yaml 重命名，合并 build 与网络配置，服务名 tiger-archive；移除两个叠加文件。只用 docker compose config 检查默认及外部网络模式，没有构建或启动服务。

本轮全量 `node --test tests/*.test.cjs`：141/141 通过，约 14 秒。

## 2026-09-10：tank 命名与统一部署

项目 Compose 的服务、容器、镜像命名统一为 tank。统一部署文件新增 tank 服务，复用其 Redis/默认网络，使用未与现有配置冲突的 3007:8080。两个 Compose 均通过 config 解析，检查了镜像、端口、依赖、网络和固定 Redis 前缀；没有执行构建或启动服务。

## 2026-09-10：统一 Nginx 游戏入口

按用户选择接入 https://mh33.top:3007，统一 Compose 将该端口从 tank 直连改为 Nginx TLS，设置 tank 的 PUBLIC_ORIGIN，并挂载项目 nginx/ 配置。Compose 解析及端口唯一性、配置文件挂载、Origin、代理升级头与 Docker DNS 指令检查通过；没有运行 nginx -t、容器启动或真实 TLS/WebSocket 测试。

## PWA 验收清单（在线版）

- [ ] HTTPS 部署后可以安装到桌面，从图标以独立窗口启动。
- [ ] 首次成功联网安装 Worker 后，关闭并重新打开应用；断网导航显示离线提示页，重试链接在网络恢复后可进入游戏。
- [ ] 游戏代码、模型、音频与 API 不进入 Service Worker 缓存；HTTP 缓存沿用服务器 no-cache 校验。
- [ ] 缓存仅包含 tiger-field-online-v2 的离线提示页；旧 tiger-field-* 缓存清理，其他应用缓存不受影响。
- [ ] 更新不强制刷新对局，关闭该站点所有窗口/标签后重新打开才切换等待中的 Worker。
- [ ] iOS 添加到主屏幕后的图标与独立窗口、Android 安装入口仍需真机验证。安装提示与横屏锁定不保证由浏览器自动触发。

## 2026-09-11：平台四周自由下落

移除指定出口限制，上层四周围栏改成地面警示线，HUD 与小地图同步。新增四种单位、两层平台、四边与对角方向的实际驾驶覆盖，以及边缘探出、底楼边界/掩体阻挡和下层边缘着陆测试。未启动服务或执行构建，浏览器视觉与真实联机仍待实测。

本次全套测试：147 项中 142 通过、5 失败。9 项下落专项全部通过；失败集中在已有 Service Worker 被列入普通页面脚本清单后引起的 VM self 未定义（4 项）及混淆清单预期不一致（1 项），相关文件本次未修改。

## 2026-09-11：PWA 修复

修复 CSP 阻止内联注册、Worker 误作页面脚本、旧核心规则缓存、安装失败被吞、清理其他应用缓存、更新打断对局及缺失 iOS 图标引用。明确在线 PWA 范围，离线提供独立重试页，发布清单和 Docker COPY 已补齐。151/151 自动化测试通过，含四项 PWA 专项（资源/图标、安装与清理、网络旁路与离线回退、注册行为）。未运行发布构建、Docker 或开发服务；真实浏览器安装和旧 Worker 升级迁移仍待部署验收。

## 2026-09-11：后坐力与音量

新增武器差异化视觉后坐、连射上限、指数回弹及不改变中心视线的镜头后移。短音效约提高 1.8 倍、履带声提高 1.5 倍。全套 153 项首次运行 152 通过，剩余一项因独立 VM 测试未加载新增 recoil 依赖失败；补齐测试依赖后，相关 14 项全部通过（含该失败项）。覆盖镜头回弹/方向保持、权威状态不变、减少动态效果、坡底碰撞、音频静音与目标高亮。git diff --check 通过；未启动服务、打包或执行浏览器视听验收，实际力度和响度仍需体验。

## 2026-09-11：取消镜头后坐

按反馈彻底移除开火镜头位移、冲量与参数，保留模型后坐；炮座后移跟随射击方向，人类武器行程减小。相关 12 项测试通过，涵盖镜头位置/方向保持、模型衰减、页面事件、瞄准与坡底镜头碰撞。未打包或启动服务。

## 2026-09-11：公网联机移动回拉修复

旧逻辑复现：快照由 50 ms 延迟到 75 ms 时，渲染位置在收包前为 4.44、收包后回到 3，发生反向跳变。改为有界队列、连续服务器 tick 播放、约 100 ms 缓冲；网络渲染不再外推越过最新位置。新增抖动、批量到包、断流停止、重连清空及 RTT 测量测试；界面展示 FPS/往返延迟/距收包时间。首次全套 155 项中 154 通过，诊断 UI 测试暴露同步测试传输的 pong 到达早于 pendingPing 登记，修复登记顺序后相关 18 项全部通过。git diff --check 通过。未启动服务或打包、未连接生产对局；首次开火是否另有 GPU/网络停顿仍需部署后结合指标判断，当前未实现本机预测。

## 2026-09-11：移除常驻护盾、激光装填 1.7 秒

四种单位常驻护盾均为 0，删除自动恢复；只有中甲技能有 60 点临时屏障，HUD 无屏障时隐藏护盾条，重甲不再显示能量盾。激光保持 100 伤害/90 tick 预热，装填改为 102 tick。157/157 测试通过，包括跨 6 秒无护盾恢复、各技能屏障归属、火箭两炮直击轻甲、激光装填边界及旧快照拒绝。已更新并检查 900 tick 基准，所有实体 shield 均为 0，击杀/复活时序随生存值及激光射速变化。README 数值表同步生成。未执行发布构建、Docker 或启动服务。

## 2026-09-11：激光炮单位穿透

激光插件升级到 2.5.0，新增 `penetratesBodies` 能力。权威射线会按距离依次命中同一直线上的所有单位，每个单位每发最多结算一次；墙体、楼板、坡面及坡道结构仍会截断射线，掩体后的单位不受伤。单条 beam 事件延伸到最近实体地形或 140 米最大射程，命中的每个单位分别产生 impact/damage 事件。协议字段不变，本次激光改动时协议常量保持 v20。

按 TDD 先加入“连续穿透两名目标并被墙体截断”的行为测试，旧实现按预期失败于第二名目标仍为满血；实现后 `tests/aim-assist.test.cjs` 5/5 通过，战斗/插件/PvE 相关测试 50/50 通过。全套 `npm test` 为 193/196；其余 3 项失败在未修改的补给/坡道与静态资源测试，使用 HEAD `8eed3ec` 临时基线工作树复跑得到完全相同的 3 项失败，确认不是本次改动引入。`npm run build:client` 成功生成 64 个混淆发布文件，验证后恢复原有 `public-dist`，未覆盖旧发布目录。

## 2026-09-11：PvE 追击与 Boss 压制优化

协议升级 v21，zombie 插件升级 1.3.0。感染者对同一玩家复用六个近战攻击位，Boss 独立直接追击；感染者之间取消硬碰撞但仍受玩家和实体地形阻挡。连续受阻 12 tick 立即丢弃旧路径，再按编号于最多 16 tick 内分帧重算，每 tick 至多一次 A*；PvE 路径缓存由 240 缩短到 90 tick。六只感染者排队追击的回归场景中，旧实现只有 `zombie_0` 产生伤害；新版至少三只可进入近战并参与攻击。

Boss 移速改为 3.4 m/s，近战距离 2.8 米，基础近战 30、间隔 60 tick；红圈预警改为 60 tick，并按权威移动与地形规则逐 tick 预测落点，包含坡面高度、墙体截停和下落惯性。普通/狂暴阶段分别为 6/7 米、45/50 伤害，攻击等待为 180/120 tick。释放预警时 Boss 短暂加速；Boss 战每 180 tick 补充一只普通或疾跑感染者，援军上限 `min(6, teamSize+2)`，仍使用原 16 个敌人槽位。

按 TDD 先确认尸群拥堵和 Boss 固定慢红圈/无援军测试失败。独立审查随后发现高编号感染者与 Boss 外圈停步、坡道红圈高度错误，以及同步受阻触发 16 次 A*；分别补充回归测试并修复为六槽复用/Boss 直追、权威地形逐 tick 预测和每 tick 一次分帧重算。实现后 PvE 与肉鸽专项 28/28 通过，PvE、环境、网络、插件、快照和页面相关 78/78 通过；全套 `npm test` 为 199/202，剩余 3 项仍是已由旧 HEAD 基线复现的补给/坡道及 Three.js 静态资源失败，本次未修改对应实现。`npm run build:client` 成功生成 64 个混淆发布文件，随后恢复原有 `public-dist`。真实多人难度、红圈视觉与墙角极端场景仍需部署试玩。

## 2026-09-11：v22 武器专属成长路线

五种武器均具有只在当前装备时生效的专属依赖链：手枪贯穿/弹射/雷电；标准炮震荡重弹/处决弹；快速炮压制弹链/交叉火力；激光炮广角透镜/等离子爆发；火箭筒扩爆/燃烧/连锁殉爆。标准炮加入 PvE 换装池，通用脉冲路线保持不变。完整检查点新增对所有节点上限与前置依赖的校验，协议升级 v22；切换武器会暂停旧武器效果但保留已选节点。

按 TDD 先加入路线覆盖、前置门槛、标准炮暴击震荡/处决、快速炮减速/交叉伤害、激光 0.75 米宽束/单发一次 3 米等离子爆发、切枪后旧弹仅保留基础伤害且不再触发标准炮或火箭专属效果、火箭切换后不误触发殉爆，以及伪造跳级检查点拒绝测试。肉鸽专项 18/18 通过，PvE/战斗/激光/插件/网络/快照/UI 相关测试 82/82 通过；全套 `npm test` 为 204/207，剩余 3 项与既有基线一致。`npm run build:client` 成功生成 64 个混淆发布文件，验证后恢复原 `public-dist`。

## 2026-09-12：v23 十六波与双 Boss

PvE 改为 16 波战役：第 8 波生成红色零号感染体，中期 Boss 击败后继续；第 9～15 波移除普通感染者并解锁重锤巨人；第 16 波生成紫色泰坦感染体，只有击败泰坦才胜利。零号感染体保持 1200 基础生命、6/7 米红圈与 45/50 伤害；泰坦为 2200 基础生命、3.4 米近战距离、9/11 米红圈、60/70 伤害和 75 tick 预警。两者使用不同外观、援军池和攻击节奏。

协议升级 v23、zombie 插件升级 1.4.0。检查点新增 `boss.stage`，严格绑定当前波次、活跃 Boss 类型、红圈半径及最终胜利状态；第 8 波 Boss 已死、第 16 波战中和泰坦已死三个阶段均通过恢复校验。PvE 时限由 8 分钟扩为 16 分钟，FFA 保持 8 分钟，HUD 根据模式显示倒计时并标识 `/16` 和当前 Boss 名称。标准炮处决节点同步覆盖泰坦。

按 TDD 先确认旧实现会在第 6 波生成重锤、波 8 不生成 Boss、没有 titan 类型且 PvE 无独立 16 分钟时限。实现后 PvE/肉鸽专项 34/34，通过插件、网络、快照、UI 和七种感染者模型相关测试 82/82；全套 `npm test` 为 205/208，剩余 3 项仍是既有补给/坡道和 Three.js 静态资源基线失败。`npm run build:client` 成功生成 64 个混淆发布文件，验证后恢复原 `public-dist`。端到端权威快进按真实生成与休整顺序经过 1～16 波，Boss 顺序为 `8:boss → 16:titan`，最终结算 `victory`，中途检查点持续通过校验。

## 2026-09-12：v24 双 Boss 检查点加固

独立审查发现 v23 检查点可伪造未击败泰坦的 `victory`，也可构造跳过第 8 波 Boss 或让中期 Boss 在第 9～15 波继续存活的不可达状态。按 TDD 新增三类负向测试后收紧校验：胜利必须绑定 `stage=2 / spawned=true / defeated=true` 和已死亡泰坦；第 8 波后必须保留已死亡零号感染体；第 9～15 波禁止活跃中期 Boss。

为避免保留 Boss 历史导致后半程并发从 16 降为 15，实体池调整为 16 个普通感染者槽位加 1 个专用 Boss 槽位；普通波同屏上限仍为 16。该快照结构变化将协议升级为 v24，zombie 插件保持 1.4.0。泰坦测试补全普通/狂暴阶段 60/70 伤害、150/90 tick 等待及 brute/runner 专属援军。PvE/肉鸽专项 35/35，协议/插件/网络/快照/UI/模型相关测试 83/83；全套 `npm test` 为 206/209，剩余 3 项仍为既有基线失败。客户端发布构建成功生成 64 个混淆文件；v24 端到端快进完整经过 1～16 波、Boss 顺序正确并最终结算胜利。

## 2026-09-12：联机 PvE 客户端帧率优化

针对 i5-9300H 笔记本联机 PvE 约 42 FPS 的反馈，分离测量了网络插值、单位同步、DOM 和 GPU 提交路径。8 人 PvE 的 25 实体快照插值约 0.063 ms/帧，单位 JavaScript 同步约 0.08 ms/帧，均不足以解释掉帧；16 只普通感染者平均约 154 个可见 Mesh / 1.31 万三角形，旧实现全部参与 2048² PCFSoft 动态阴影，僵尸本身约产生 154 次颜色绘制和 154 次阴影绘制，是主要瓶颈。瞄准辅助还会让屏幕距离 96px 内、最终不可能吸附的目标重复执行完整可见性 Raycast。

优化后普通感染者不再投射动态阴影，保留接收阴影和 boss/titan 的动态阴影，最坏普通尸群减少约 154 次阴影绘制/帧。瞄准辅助先按 16px 吸附范围或 64px 直接命中范围过滤，再执行可见性检测；已锁定目标仍保留原 96px 跟随范围。死亡/未出生僵尸跳过完整动画和标签计算，敌人标签只在值变化时写 DOM，屏幕位置限为约 30Hz。基准候选可见性检查从 5 次降至 1 次，无效标签样式写入从约 96 次/帧降至接近 0。相关测试 91/91，全套 `npm test` 为 207/210，剩余 3 项仍为既有基线失败；客户端发布构建成功生成 64 个混淆文件。

### 第一波模型不可见回归修复

首版死亡僵尸早退出在预生成槽位中写入了 `lastSpeed/lastHeading`，却没有同步初始化 `leanPitch/leanRoll/travel/dustTime`。第一波复用默认 walker 槽位时不会触发 variant 模型重建，首次存活更新遂生成 NaN 四元数；权威实体仍能移动与攻击，但 WebGL 无法渲染模型。新增“未出生 walker 槽位 → 第一波存活”真实 Three.js 回归，旧实现按预期因非有限四元数失败。修复后死亡槽位完整初始化并清零动画倾斜状态，保留早退出性能；模型/PvE/联机组合测试 96/96，全套仍为 207/210，仅有 3 项既有基线失败。

## 2026-09-12：v25 PvE 地面专用地图

PvE 默认权威地图改为一层：移除二楼、三楼、全部坡道、上层掩体/补给和下落出口；8 个原出生坐标投影到地面并全部通过碰撞验证。FFA 与本地训练仍使用完整三层地图。客户端同步隐藏上层 floor groups、坡道 terrain 和单独批次的坡道植被，摄像机碰撞、瞄准视线及 HUD 小地图均调用 `mapForMode()`。快照改为按模式校验 mapId、补给、楼层、高度和坡道状态，伪造 PvE 二楼/坡道状态会被拒绝。协议升级 v25；PvE/Boss/环境专项 43/43，包含网络、UI、摄像机和 FFA 地图的相关组合 97/99，2 项为既有 match 基线失败；全套 `npm test` 为 209/212，剩余 3 项仍为既有基线失败。客户端发布构建成功生成 64 个混淆文件。

## 2026-09-12：v26 怪物波次血量成长

怪物最大生命在人数倍率外，每波线性增加 5%；第 8 波为 135%，第 16 波为 175%。波次切换、Boss 生成、援军生成、玩家正式离场重算和检查点 maxHp 校验全部使用同一 `healthFor(type, teamSize, wave)`。保留原有近战每波 +2、最多 +16 伤害，不增加移速、攻速、范围或红圈数值。新增单人第 8 波 Boss 1620 血、8 人第 16 波泰坦 10588 血及伪造旧血量快照拒绝回归。协议因快照合法血量变化升级为 v26。协议/UI/PvE 组合 81/81；完整 16 波权威快进经过 1～16 波并正常 victory；全套 `npm test` 为 210/213，3 项仍为既有基线失败。

独立审查发现 Three.js Raycaster 仍会命中 `visible=false` 父组下的坡道 Mesh，使激光等蓄力警示线可能被 PvE 隐藏坡道截断。新增隐藏父组与 `aimIgnore` 近障碍、可见远障碍的线段终点回归；警示线和 `aimHit()` 现共用祖先可见性过滤。修复后瞄准/UI/PvE/环境/发布组合测试 39/39。

### 火箭有效爆炸范围扩大

玩家实测反馈爆炸视觉明显大于有效伤害范围。rocket 插件升级至 1.0.4，基础爆炸半径从 6 米扩大至 8 米；PvE「扩爆弹头」仍每级增加 1 米，三级后为 11 米。直击 20、中心爆炸 80 及线性距离衰减保持不变。新增 8 米基础范围、切枪后 8 米基础事件及三级 11 米事件回归。
