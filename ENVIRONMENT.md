# 卡通维修基地场景

首版用 Blender 制作车库、货柜和能源设备，替换现有战场的大块掩体外观，并增加地面接缝、车道虚线、停车线、维修区标记和掩体边界线。采用奶油白、青蓝、橙黄与深色金属的纯色低面数风格。模型为本项目生成，未复制参考网站资产。

这是维修基地主题的场景样板，仍沿用原有三层竞技场布局。尚未完成完整岛屿地形、建筑内部、环境互动或参考网站级别的美术打磨。

## 文件与磁盘占用

| 文件 | 用途 | 大小 |
|---|---|---:|
| art/maintenance-base.blend | 可编辑 Blender 原始场景，含三种建筑与预览相机 | 182,871 字节，约 179 KiB |
| client/environment/maintenance-kit.json | 网页实际加载的 Three.js ObjectLoader 模型 | 586,371 字节，约 573 KiB |
| scripts/create-maintenance-scene.py | 程序化建模与导出脚本 | 纯文本 |
| client/environment.js | 模型复用、原掩体替换和地面标线 | 纯文本 |

两个模型文件合计 769,242 字节，约 751 KiB。不使用贴图，不新增依赖，也没有下载素材包。三种原型共 6,052 个三角形、18 个材质网格；同型建筑共享几何体和材质。地面标线按楼层与颜色使用 InstancedMesh 合批。模型尺寸不代表整局运行内存或帧率。

Docker 上下文排除 art/ 和 docs/；运行镜像只需要 client/ 下的模型，服务器无需安装 Blender。Blender 的软件安装空间与本项目文件大小分开计算。

## 编辑与导出

在 Blender 中打开 art/maintenance-base.blend 可检查和手动编辑模型。源文件内的三个建筑已摆开用于预览；预览地板、灯光和相机不导出到游戏。

需要从脚本重新生成时，可在项目根目录手动执行：

```sh
/Applications/Blender.app/Contents/MacOS/Blender --background --factory-startup --threads 2 --python scripts/create-maintenance-scene.py
```

**这条命令会重建并覆盖上述 .blend 和 JSON，不能用于导出已手工修改的 .blend。** 手工修改前请另存文件；当前可重复的生产路径是修改脚本中的造型参数后重新生成。脚本不执行图像渲染，不进入开发监听，也不生成贴图缓存。脚本关闭本次生成的 .blend1 备份；不更改全局已保存偏好。

导出格式为 Three.js 原生对象 JSON，包含归一化 BufferGeometry、法线、索引和 MeshToonMaterial，直接使用项目现有 Three.js 0.158.0 的 ObjectLoader，无需 GLTFLoader 或压缩器。各原型 x/z 范围约为 [-0.5, 0.5]、y 为 [0, 1]，由客户端按共享地图障碍尺寸缩放。文字有不足 0.001 的归一化表面突出。

## 玩法与加载

- 权威障碍数据仍在 core/map.js。建筑按原碰撞盒放置；车库卷帘门保持关闭，避免出现“看着可进入，实际撞墙”的入口。
- 坡道、楼板开口、坠落路径和战斗规则不变，协议仍为 v13。
- 先显示基础掩体，再异步加载建筑外观；请求失败、数据异常或 8 秒超时保留基础掩体，游戏不依赖模型加载完成。
- app-manifest.js 列出新增脚本与模型，开发静态白名单和发布资源清单共同读取；JSON 按 application/json 返回，.blend 不对外提供。
- 正在运行旧 Node 进程时需要重新启动以更新静态白名单，再刷新页面。部署发布模式时按 DEPLOY.md 的原流程手动重新构建镜像；本次未运行构建或启动服务。

## 验证

125/125 自动测试通过，覆盖原战斗回归与新增模型边界、关闭的建筑正面、共享几何、地面标线不跨坡道开口、加载失败回退、HTTP JSON 内容及发布清单。已在 Blender 中打开检查三种模型。尚未进行本版实际浏览器 WebGL 视觉与帧率验收；自动测试使用渲染器替身，不能代替浏览器检查。
