# Quaternius 卡通林地地图

使用第一套推荐的 **Stylized Nature MegaKit / Standard** 免费素材，替换维修基地的建筑与停车标线。树木、花草、岩石为 Quaternius 原始模型；地面、沙土路径、远山与云朵由游戏场景代码生成。来源及 CC0 许可保存在 `art/nature-public/`。

## 地图与规则

- 地面由 128×128 扩至 **256×256**：长宽各两倍，面积四倍。中央两个上层仍为 92×92，保留坡道、楼板开口、下落规则与原开局出生点。
- 外围增加环形沙土路径、20 个岩石掩体、48 棵有树干碰撞的树和四个补给点，允许两侧绕行。角色、武器和移动速度不随地图放大。
- 共享地图 `core/map.js` 同时供客户端、AI 和权威服务器使用。石头使用与模型外包围一致的简化矩形碰撞，树干使用 1.4×1.4×5 的简化碰撞；枝叶、花草为装饰，不挡弹。石头曲面与简化碰撞并不逐三角形吻合。
- 弹丸存活边界、快照坐标、PvE 技能区域与小地图缩放均适配新范围。地图 ID 为 `woodland-crossfire-v5`，协议 v20；旧地图检查点不兼容。
- 自然素材没有改变中央三层竞技场的基本结构；这是现有地图的林地改造，并非 Bruno Simon 网站的完整复刻。

## 林地坡道

四条坡道采用沙土中央路面、草边、低石缘和两端落地的岩土支撑；喇叭形入口连接上下平台。坡道长度、高差和12米通行宽度保持原值，保留中部横向涵洞与三层路线。

`core/math.js` 的 `rampSupports` 同时定义客户端土台和服务器实体碰撞；新增土台能挡弹、挡车。坡面的几何高度与服务器线性高度一致，避免原旋转薄板的表面偏差。取消发光入口圈，并加入沿坡花草。

`client/ramp-terrain.js` 生成地形并合并同类网格，每条坡道主体7个网格。预览见 `art/nature-public/ramp-preview.png`（地面层视角；上层楼板按地面游戏视角隐藏）。本次坡道改动通过73项测试，包括全车型上下坡、涵洞、AI、快照、坠落、模型加载与界面，以及新增坡面高度、土台射线和入口落点检查。没有运行打包或服务。

## 文件与加载

| 文件 | 用途 |
|---|---|
| `client/environment/nature-kit.json` | 约 4.9 MiB；8 个模型与7张共享内嵌贴图 |
| `client/environment.js` | 模型加载、岩石替换、确定性植被布局、实例化合批 |
| `client/scene.js` | 草地、路径、坡道、边界、光照与远景 |
| `scripts/assets/convert-nature.mjs` | 原始 glTF 到原生 Three.js JSON 的离线转换 |
| `art/nature-public/README.md` | 来源、原始 ZIP 校验值与转换说明 |
| `art/nature-public/LICENSE.txt` | 原始包附带的 CC0 许可 |
| `art/nature-public/preview.png` | 实际 Chrome WebGL 渲染的林地近景 |

贴图缩至最多 512×512，省略法线贴图。相同植物共享几何、材质和贴图，并使用 InstancedMesh；没有新增运行时依赖。完整下载包和未用模型不进入项目。

先显示可用的基础掩体；请求失败、模型缺失或 15 秒请求超时均保留回退。使用 ObjectLoader.parseAsync 等待内嵌图像解码，完整场景准备好后才替换。旧 `maintenance-kit.json` 已移除运行资产与清单，历史 Blender 源文件和生成脚本保留。

正在运行的旧服务进程需要重启才能读取新地图与资源清单，再刷新客户端；发布模式需按项目原流程更新发布资源。本次**没有运行构建、打包、开发服务或监听**。

## 验证

- 116 项 Node 回归全部通过，覆盖战斗、坡道、下落、AI、联机快照、PvE、瞄准与界面，以及新素材的尺寸、实例复用、失败回退、外围弹丸、外围寻路和补给可达性。
- 900 tick 固定战斗基线的实体、伤害、事件、弹丸状态未变；基线只更新地图 ID 和四个补给点。
- 使用 Chrome 对本地文件拦截响应进行实际 WebGL 渲染，无 HTTP 开发服务器；检查总体视图和地面近景、模型解码与浏览器错误。
- 预览不代表所有设备的帧率或八人真实网络延迟验收；没有运行发布构建。
