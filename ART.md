# 卡通外观试版

![模型几何离线预览](docs/cartoon-lineup.png)

这张图按游戏实际几何生成，采用离线投影与简化光照，不是浏览器游戏截图。

## 外观

- 游隼：六轮、窄炮塔、尾部双推进器，明亮配色与发光车灯。
- 先锋：履带、平衡比例的炮塔与奶油白护板。
- 堡垒：宽炮塔、三段侧装甲，厚重轮廓。
- 人类：大头盔、发光面罩、护膝和背包，保留原步行动画。

车体是本项目程序化模型，未使用 Quaternius 的双足机甲。武器来自 Kenney，安装到既有武器枢轴；炮塔跟随瞄准、炮管俯仰、后坐、激光蓄力和红色目标反馈沿用现有渲染流程。当前体积、HP、伤害、技能、碰撞与移动规则不变，仍支持 16 种组合。机甲仍按车辆驾驶，不新增双足步行机制。

## 素材来源与许可

[Kenney Blaster Kit 2.1](https://kenney.nl/assets/blaster-kit)，作者 Kenney，CC0，可用于个人及商业项目。原许可保存在 `client/art/KENNEY-LICENSE.txt`；生成文件头也保留来源。

| 武器 | 原始模型 | 三角形 |
| --- | --- | --- |
| 标准炮 | blaster-b.obj | 736 |
| 快速炮 | blaster-j.obj | 1036 |
| 激光炮 | blaster-r.obj | 988 |
| 火箭筒 | blaster-h.obj | 592 |

原包链接：<https://kenney.nl/media/pages/assets/blaster-kit/261d80a716-1753959510/kenney_blaster-kit_2.1.zip>

SHA-256：`91e3093e95427d59625e7e2ce2d0399b861600160fd0b4ada7714796b67cea8c`

选取 OBJ、法线和调色板贴图后，将颜色烘到顶点属性，保留索引，转换前向并归一化长度。`scripts/import-blasters.py` 可由安装 Pillow 的 Python 重现导入：

```sh
python3 scripts/import-blasters.py /path/to/kenney_blaster-kit_2.1.zip
```

这是独立的美术导入操作，不属于应用启动或发布构建步骤；生成的 `client/art/blasters.js` 已提交到工作目录。游戏不需要 Python、Pillow、OBJ 加载器或新增运行时依赖。

## 体积与渲染预算

四把武器的几何数据合计约 154 KiB（本地 gzip 测量约 21 KiB；当前服务是否压缩取决于部署配置，不能视为实际传输量）。只随网页加载，不进入 WebSocket 快照。无额外纹理请求。

静态装甲按父节点和材质合并，动画枢轴独立保留。16 种搭配各自少于 5000 三角形、最多 26 个可见网格，不含护盾启用、弹丸、特效和阴影的额外绘制。此预算不是帧率保证，实际 WebGL 仍需刷新后验收。

新增几何走 `app-manifest.js` 的既有资源管线，因此源码模式和 Docker 发布模式都包含它。发布时原样复制 CC0 数据，业务代码继续混淆。

## 更新与验收

外观钩子变动已提升插件补丁版本：激光 2.2.1；其余原五个 2.1.1；人类与火箭筒 1.0.1。协议仍为 v12。更新后重启服务器并刷新全部客户端，新建房间；旧插件清单的检查点不兼容。release / Docker 由部署者重新构建，本轮未自动构建。

实际游玩时检查四类车体、四把武器，尤其是转向、抬炮、坡上射击、人类步行、死亡/复活材质和红色瞄准反馈。炮口测试覆盖水平静止姿态；现有俯仰枢轴、坡上补偿和装饰几何与权威简化碰撞体仍有视觉差异，不等于逐三角形命中。
