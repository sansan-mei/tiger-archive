# 前端发布混淆

开发源码保持原样，`scripts/client-release.cjs` 从 app-manifest.js 获取浏览器文件清单，生成独立的 `public-dist/`。混淆在本地构建进程运行，不上传源码到在线服务。javascript-obfuscator 固定为 5.7.0，仅作为开发/构建依赖。

## 保护范围

所有自有浏览器 JavaScript 都会处理，包括核心模拟、插件、输入、镜头、渲染、房间列表、会话与加载器。Three.js 原样复制；HTML、CSS 与音频二进制资源原样复制；服务端私有文件不进入发布目录。Node 的权威模拟继续使用原始文件。

启用局部标识符重命名、压紧代码和字符串数组。各文件使用不同的辅助标识符前缀；不重命名跨模块全局 API、对象属性、插件 ID 或网络字段，不生成 source map。发布服务禁止访问服务端源码及 `.map` 文件，发布文件缺失会拒绝启动，不回退到源码目录。

为控制运行开销，不启用控制流打乱、死代码注入、字符串加密、反调试定时器、自防御和 eval。选项参考 [javascript-obfuscator 官方文档](https://github.com/javascript-obfuscator/javascript-obfuscator#javascript-obfuscator-options)。即使采用轻量配置，最终体积、加载速度和游戏帧率也需要以发布版本实测为准。

## 使用方式（以下命令由部署者执行）

本地开发仍用 `pnpm run start`，默认提供可读源码。

发布前安装包含开发依赖的依赖集，然后执行：

```sh
pnpm run build:client
pnpm run start:release
```

首条生成发布文件，第二条以 `ASSET_MODE=release` 启动。已有 public-dist 时生成器拒绝覆盖；先停止使用旧文件的发布服务，将旧目录移走或确认后删除，再生成新版本。临时目录构建成功后一次性改名为 public-dist，失败时只清理本次临时目录，不修改源码。

Docker 已接入多阶段构建，默认 `OBFUSCATE_CLIENT=true`。正常更新镜像就会执行混淆；运行阶段只安装生产依赖，以发布模式提供静态文件，构建工具不进入最终运行依赖。此配置不代表已经执行镜像构建。

排查发布兼容问题时，可在 Compose 使用的 `.env` 中设置：

```dotenv
OBFUSCATE_CLIENT=false
```

然后重新构建镜像。它是**构建参数**，仅重启旧镜像不会改变混淆状态。false 会生成可读前端副本，排查结束后改回 true 并重建。不要把可读调试版本当作受保护版本。

## 验证与边界

轻量单元测试只混淆少量内存示例，验证跨文件 API、中文文本、JSON 字段和禁用 eval 的执行环境。测试也检查发布文件清单、私有文件排除及发布路径不回退。普通测试不会自动生成整个游戏发布目录或构建镜像。

正式发布验收应使用混淆版本检查：16 种单位/武器组合、激光蓄力、火箭爆炸、鼠标与坡道、房间列表、创建/加入、重连及浏览器帧率；不要只测源码模式。本次尚未执行完整混淆构建和发布浏览器验收。

混淆提高直接阅读、复制和修改的成本，不是加密，也不能阻止逆向或作弊。联机移动、伤害、冷却和命中仍由服务器校验；本地训练运行在玩家浏览器里。不要在任何前端版本嵌入密码、Redis 地址凭证或私钥。
